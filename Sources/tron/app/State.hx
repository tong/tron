package tron.app;

class State extends Trait {

    function new() {
        super();
    }

    /* inline function goto( state : Class<State> ) {
        setState( Type.getClassName(state).toLowerString() );
        //if( scene != null ) Scene.setActive( 'app.$scene' );
    } */

}
